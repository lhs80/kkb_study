一、栈的方法
    1、push 添加一个元素到栈顶（栈的元素个数加1）;
    2、pop  弹出栈顶元素(栈的元素个数减1)；
    3、top  返回栈顶元素（不是弹出，栈的元素个数不变）；
    4、isEmpty   判断栈是否为空 ；
    5、size  返回栈里元素的个数；
    6、clear 清空栈；
二、栈的实际应用
    1、中缀表达式；
    2、后缀表达式；


四、树
    1、